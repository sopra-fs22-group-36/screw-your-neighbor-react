import React, { useEffect, useState } from "react"
import BaseContainer from "../ui/BaseContainer"

const Lobby = (props) => {
  return (
    <BaseContainer>
      <h1>das ist die Lobby</h1>
    </BaseContainer>
  )
}

export default Lobby
